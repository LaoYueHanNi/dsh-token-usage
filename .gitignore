node_modules/
# Build output — enters the npm tarball via prepublishOnly, never the repo (see docs/decisions/)
lib/
*.tsbuildinfo
.npm-cache/

# IDE / editor config
.idea/
.vscode/
.zcode/
.fleet/

# OS junk
.DS_Store
Thumbs.db

# Machine-local temporary-mount patch for `dsh web --patch` (see cordis.example.yml)
cordis.yml

# Machine-local agent instructions (never commit)
.claude/
AGENTS.local.md
CLAUDE.local.md
